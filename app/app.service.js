var app = angular.module('appModule');

app.service('SendService', function($http){

    /*
    this.sendPost = function (postObj) {
        $http({method: 'GET', url: _url})
            .then (function (data) {
                return data;
            });
    };

    this.sendNews = function (newsObj) {
        $http({method: 'GET', url: _url})
            .then (function (data) {
                return data;
            });
    };
    */
});
